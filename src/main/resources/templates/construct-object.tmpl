[# th:each="attribute : ${item.allAttributes}"]
        if (map.containsKey("[(${attribute.name})]")) {
            builder.[(${attribute.attribute})]([(${item.name})].[(${attribute.helper})].extractFromMap(map));
        }
[/]

